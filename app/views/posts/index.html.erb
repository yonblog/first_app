<h1>Top page</h1>
<%= link_to '新規投稿', '/posts/new' %>
<% @posts.each do |post| %>
  <div class="post">
    <div class="post_date">
      投稿時間：<%= post.created_at %>
    </div>
    <div class="post_content">
      <%= post.content %>
    </div>
  </div>  
<% end %>
